# RC14.21 QA Report

Version: `4.4.21-rc14.21-longform-eta`
Branch: `rc14.21-preview`

## Gate summary

- GitHub RC14.21 Verify: PASS
- Legacy RC14.19 broken asset-pack rejection: PASS
- Exact bundle reconstruction SHA256: PASS
- Node syntax: PASS
- Inline UI JavaScript syntax: PASS
- RC14.21 invariants: PASS
- Vercel Preview deployment: READY
- Vercel Preview health: confirmed `200` with exact RC14.21 version before Preview Protection re-challenged subsequent requests
- Production OpenAI status (existing production environment only, no RC14.21 promotion): `configured=true`, `reachable=true`, model `gpt-4o-transcribe-diarize`
- Production worker status (existing production environment only): `ready=true`, `ffmpeg=true`, `japaneseFont=true`, `resumable=true`
- Production `start-job-smoke`: returns `401 owner_access_required`, matching code design outside Preview
- Short render prototype: PASS, Media QA `passed=true`
- Resume/ETA test: PASS
  - forced 8-second render blocks, 5 blocks total
  - intentional stop after block 1
  - cached block 0 retained identical SHA256, size and mtime after resume
  - ETA observed from 43 seconds remaining to 0
  - render cache reuse confirmed
- 34-second / 5-block resumed render: PASS
  - final stage `complete`, progress `100`
  - `completedRenderBlocks=5/5`
  - `renderAverageSeconds=8.248`
  - `renderEtaSeconds=0`
  - `inputTrimmedRenderBlocks=true`
  - `tinyTailMerged=true`
  - `longFormEta=true`
  - Media QA `passed=true`
  - H.264 1280x720
  - AAC 48kHz stereo
  - final duration 43.072s
  - max volume -5.6 dB, mean -16.1 dB
  - 3 thumbnails
  - MP4 / MP3 / WAV / TXT / SRT / YouTube description / chapter export generated

## Remaining production gate

Production promotion remains blocked only because Vercel Preview Protection prevents the current automation client from directly completing the protected Preview calls to:

- `/api/openai-status`
- `/api/worker-status`
- `/api/start-job-smoke`

The Preview deployment itself is READY and the exact RC14.21 artifact is verified. Do not promote to production until the protected Preview smoke is completed through an authenticated browser/session or an equivalent trusted route.
