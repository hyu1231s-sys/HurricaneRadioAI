# RC14.21 Longform ETA — Release Record

Version: `4.4.21-rc14.21-longform-eta`
Stable branch: `stable/rc14.21`
Validated preview deployment: `dpl_DxiDX4njpqFStBJHWb6crrEpQZm8`
Validated preview URL: `https://hurricane-radio-7wzx01bx1-hyu1231s-2033s-projects.vercel.app`

## Verified

- GitHub RC14.21 reconstruction and SHA verification: PASS
- Node syntax and inline UI JavaScript syntax: PASS
- RC14.21 invariants: PASS
- Vercel Preview deployment: READY
- Preview `/api/health`: RC14.21
- Preview `/api/start-job-smoke`: HTTP 200 / PASS
- Sandbox bridge and transcript roundtrip: PASS
- Resumable render cache reuse: PASS (completed part hash/mtime/size unchanged)
- ETA convergence: PASS
- Short render: PASS
- 5-block interrupted/resumed render: PASS
- Fail-closed Media QA: PASS
- H.264 1280x720 + AAC 48 kHz stereo: PASS
- MP4/MP3/WAV/TXT/SRT/source-SRT/chapters/YouTube description/3 thumbnails: PASS
- Longform input trimming and tiny-tail merge invariants: PASS
- Production OpenAI status before promotion: configured/reachable
- Production worker status before promotion: ready/ffmpeg/japaneseFont/resumable

## Production promotion

Production is intentionally not considered promoted until `https://hurricane-radio-ai.vercel.app/api/health` reports exactly `4.4.21-rc14.21-longform-eta`.

The workflow `.github/workflows/promote-rc1421.yml` promotes the already validated Preview without rebuilding. It requires the repository Actions secret `VERCEL_TOKEN` and can be run manually with `workflow_dispatch`.

After promotion, verify in order:

1. `/api/health` exact RC14.21 version
2. `/api/openai-status` configured/reachable
3. `/api/worker-status` ready + ffmpeg + Japanese font + resumable
4. Short production render
5. Longform production render / resume / ETA / Media QA

## Rollback

If production verification fails, do not continue processing new longform jobs. Point production back to the previous known-good deployment, while source recovery is available from branch `stable/rc14.21` and the retained RC14.21 source bundle.
